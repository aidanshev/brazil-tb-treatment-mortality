# Frozen code package required

The original frozen model-development code package was not available to this runtime. Do not substitute a newly written implementation and label it original frozen code. Recover/verify the original package, exclude raw SINAN/private paths/images, run tests, then update status.
